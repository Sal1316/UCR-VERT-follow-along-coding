{{!--  --}}
<!-- For each gallery, the 'gallery-details' partial is used to render the gallery data -->

{{#each galleries as |gallery|}} 
      {{> gallery-details}}   {{!-- the partial is accessed through the '>' and is the partial of gallery-details.handlebars --}}
{{/each}}hi