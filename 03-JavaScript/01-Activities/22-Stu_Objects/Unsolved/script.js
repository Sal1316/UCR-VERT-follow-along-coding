//WRITE YOUR CODE BELOW

var customerDrink = {
      name: "blue bubblie",
      numSugars: 4,
      ready: true,
}

console.log( customerDrink.name)
console.log( customerDrink.numSugars)

if (customerDrink.ready === true ) {
      console.log("Ready for pick-up")
} else {
      console.log('Still in order queue')
}
