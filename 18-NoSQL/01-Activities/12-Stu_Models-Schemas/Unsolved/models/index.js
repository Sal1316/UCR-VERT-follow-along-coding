const Book = require("./Book");

module.exports = { Book };
/* we are importing the book they exporting it in the  index file. this is bc
      the index file is the first one that we look for in the folder.*/
