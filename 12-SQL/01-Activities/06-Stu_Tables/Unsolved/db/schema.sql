DROP DATABASE IF EXISTS books_db;
-- Creates the "books_db" database --
CREATE DATABASE books_db;

USE books_db; -- selects the db for use --

-- Add biography table --
CREATE TABLE biographies() {
      id INT NOT NULL, -- name of col, and data type, & other criteria
      name VARCHAR(100) NOT NULL -- siries of chars that cannot be more thatn 100 in lenght --
};
     


